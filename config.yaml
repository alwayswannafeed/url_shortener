log:
  level: debug
  disable_sentry: true

db:
  url: "postgres://token:token@db:5432/url_shortener?sslmode=disable"

listener:
  addr: :8000

cop:
  disabled: true
  endpoint: "http://..."
  upstream: "http://..."
  service_name: serv1_svc
  service_port: 80
